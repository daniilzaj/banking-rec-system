# --- Environment Configuration ---

# Log level can be DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL="DEBUG"

# Paths are relative to the data-ai-service root folder
INPUT_DIR="data_input"
OUTPUT_DIR="data_output"